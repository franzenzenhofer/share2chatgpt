{
  "name": "share2chatgpt",
  "version": "1.1.0",
  "private": true,
  "description": "Share to ChatGPT button widget",
  "scripts": {
    "build": "node scripts/prepare-dist.mjs",
    "check": "node scripts/check.mjs",
    "deploy": "npm run check && npm run build && wrangler pages deploy dist/ --project-name=share2chatgpt --commit-dirty=true && npm run verify",
    "verify": "node scripts/verify.mjs",
    "serve": "npx -y serve dist/ -p 8082"
  }
}
